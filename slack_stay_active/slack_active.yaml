---
config:
  # Do not move the mouse if we disable functionality:
  enabled: true
  # Output feedback to the console:
  debug: true
  slack:
    org_url: https://app.slack.com/client/<blabla>
    workspace: yourworkspace
    username: you@me.com
    password: secret